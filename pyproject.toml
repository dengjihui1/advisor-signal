[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "advisor-signal"
version = "0.1.0"
description = "Evidence-grounded PhD supervisor matching and outreach packets"
readme = "README.md"
requires-python = ">=3.9"
license = {file = "LICENSE"}
authors = [{name = "Zihui Deng"}]
dependencies = []

[tool.setuptools.packages.find]
include = ["advisor_signal*"]
